# 런타임 의존성.
# ⭐️ 상한을 두는 이유: Flask 4 / Werkzeug 4 는 확실히 깨는 변경을 예고하고 있고,
#    이 앱은 세션 쿠키(itsdangerous)·정적 서빙·에러 핸들러처럼 메이저 업그레이드에서
#    동작이 바뀌는 지점을 직접 건드린다. 하한은 실제로 검증한 버전이다.
#    올릴 때는 requirements-dev.txt 로 테스트를 돌린 뒤 상한을 넓힌다.
Flask>=3.1.3,<4.0
Werkzeug>=3.1.8,<4.0
waitress>=3.0.2,<4.0

# --- 휴장일 -----------------------------------------------------------------
# ⭐️ KRX 휴장일을 사람이 관리하지 않기 위해 쓴다. 음력 환산(설날·추석)과
#    대체공휴일 규칙을 이 패키지가 계산한다. 여기에 거래소 고유 휴장일
#    (근로자의 날 5/1, 연말 폐장일 12/31)만 prices.py 가 얹는다.
#    [주의] 임시공휴일 반영에는 갱신이 필요하지만, 기동 경로에서 자동
#    업그레이드하지 않는다 — 휴장 판정 라이브러리가 기동 때마다 조용히 바뀌면
#    장 운영시간 판단이 사람 모르게 달라진다.
#    갱신은 tools/update_holidays.sh 를 주기 작업(주 1회 cron)으로 돌려 분리한다.
holidays>=0.103
