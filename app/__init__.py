"""애플리케이션 패키지 — 루트에 흩어져 있던 모듈을 성격별로 묶는다.

- `app.routes`   : 웹 라우팅 (Blueprint 등록, 요청/응답 미들웨어)
- `app.services` : 비즈니스 로직 (시세, 통계, 뉴스, 이미지, 백그라운드 잡, 도메인 규칙)
- `app.database` : 데이터 접근과 스키마 (커넥션, 마이그레이션, 항목 정합성 규칙)
- `app.utils`    : 횡단 관심사 (로깅, 레이트리밋, 캐시)

이 패키지 자체는 **부수효과가 없어야 한다.** 임포트 순서에 따라 동작이 달라지면
안 되므로 여기에 재수출(re-export)을 두지 않는다.
"""
