[Korean](README.md) | [English](README.en.md)

# 주식 매매 일지 (Stock Trading Journal) - Multi-User Edition

[![License](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)

개인 투자자를 위한 **로컬 기반 주식 매매 일지 및 포트폴리오 관리 웹 애플리케이션**입니다.
파이썬(Flask) 백엔드와 SQLite를 기반으로 하며, **다중 사용자(Multi-User) 환경**과 **모바일 기기 최적화**가 적용되어 언제 어디서든 안전하고 쾌적하게 매매 기록을 관리할 수 있습니다.

---

## 목차 (Table of Contents)
1. [프로그램 개요 및 목적 (Overview & Objective)](#1-프로그램-개요-및-목적-overview--objective)
2. [주요 기능 상세 (Key Features)](#2-주요-기능-상세-key-features)
3. [필수 요구 사항 (Prerequisites)](#3-필수-요구-사항-prerequisites)
4. [설치 및 실행 가이드 (Installation & Execution)](#4-설치-및-실행-가이드-installation--execution)
5. [데이터 백업 및 보안 가이드 (Backup & Security)](#5-데이터-백업-및-보안-가이드-backup--security)
6. [프로젝트 구조 (Project Structure)](#6-프로젝트-구조-project-structure)
7. [시스템 트레이딩 연동 API (Trading API)](#7-시스템-트레이딩-연동-api-trading-api)
8. [테스트 가이드 (Testing)](#8-테스트-가이드-testing)

---

## 1. 프로그램 개요 및 목적 (Overview & Objective)

이 프로그램은 개인 투자자가 엑셀이나 수기 노트 대신 **웹 브라우저를 통해 직관적이고 체계적으로 매매 일지를 작성하고 포트폴리오를 관리**할 수 있도록 돕는 도구입니다.
단순한 기록을 넘어, **매매 성과 분석(승률, 손익비 등), 캘린더 뷰, 실시간 현재가 및 뉴스 연동** 기능을 제공하여 투자 복기와 전략 수립을 극대화하는 데 목적이 있습니다.

---

## 2. 주요 기능 상세 (Key Features)

*   **다중 사용자 & 보안 (Multi-User & Security)**
    *   회원가입 및 로그인 기능을 제공하며, 최초 가입자가 자동으로 최고 관리자로 지정되어 이후 가입자에 대한 승인 제어가 가능합니다.
    *   로그인 5회 실패 시 1분간 계정 차단, 1시간 장기간 미활동 시 5분 전 경고 및 자동 로그아웃 처리.
    *   대용량 파일 업로드 방어(16MB 제한) 및 XSS, CSRF 보안 조치 등 안전한 금융 데이터 보호가 적용되어 있습니다.
*   **매매 일지 & 스마트 에디터 (Journal & Smart Editor)**
    *   매수, 매도, 관망, 배당 기록뿐 아니라 종목에 대한 일반 메모와 아이디어를 기록할 수 있습니다.
    *   에디터 본문 내에 이미지 직접 삽입(스크린샷 클립보드 붙여넣기, 드래그 앤 드롭 지원) 및 크기 조절이 가능하며, 외부 텍스트 붙여넣기 시 색상/배경 서식을 자동 정제합니다.
*   **포트폴리오 대시보드 & 현재가 연동 (Dashboard & Real-time Price)**
    *   현재 보유 종목, 총 투자 금액, 평균 단가, 누적 실현 손익을 자동 계산하고 시각적인 파이 차트로 제공합니다. (사용자 지정 드래그 앤 드롭 정렬 지원)
    *   1분 주기 현재가 자동 업데이트를 지원하며, 정규장(KRX) 뿐만 아니라 **시간외 단일가(NXT)** 캐싱 및 토글 기능을 완벽하게 지원합니다.
    *   청산 종목 보기, 현재가 보기 등의 옵션 상태가 사용자별 설정(Preferences)에 동기화됩니다.
*   **5중 독립 필터링 (Advanced Filtering)**
    *   기록별, 종목별, 계좌별, 증권사별, 투자 분류별 5개의 독립된 필터를 통해 방대한 매매 기록을 신속하고 정밀하게 필터링하여 분석할 수 있습니다.
*   **매매 성과 분석 & 차트 뷰 (Performance Analytics)**
    *   승률, 손익비(Profit Factor), 평균 수익/손실, 최대 낙폭(MDD), 평균 보유기간 등 회고를 위한 심도 있는 통계 지표를 제공합니다.
    *   월간/주간 단위 실현 손익, 평가 손익, 매매 금액, 누적 수익금 흐름을 보여주는 인터랙티브 막대 차트를 지원하며, 차트 막대 클릭 시 해당하는 세부 거래 내역을 바로 확인할 수 있습니다.
*   **데이터 무결성 검증 (Data Integrity)**
    *   보유 수량을 초과하는 매도나 미보유 종목에 대한 매도 등, 논리적으로 잘못된 데이터 입력을 서버 단계에서 원천 차단하여 평균 단가와 실현 손익의 무결성을 유지합니다.
*   **캘린더 뷰 & 엑셀 내보내기 (Calendar & Export)**
    *   월별 달력에서 일자별 매매/메모 현황과 실현 손익을 색상 테마별로 직관적으로 파악할 수 있으며, 전체 기록을 버튼 한 번에 엑셀(XLSX) 데이터로 내보낼 수 있습니다.
*   **실시간 뉴스 연동 (Live News Integration)**
    *   보유 중이거나 최근 기록한 종목과 관련된 최신 뉴스를 구글 뉴스(RSS) 기반으로 5분 주기로 로드하여 사이드바에 제공합니다.
*   **관리자 대시보드 & 시스템 로깅 (Admin & Logging)**
    *   전체 사용자 목록, 가입/최근 접속 시간, 데이터 통계를 한눈에 확인하고 사용자 상태를 관리할 수 있는 관리자 전용 대시보드를 지원합니다.
    *   접속, 작업, 에러 로그가 매일 날짜별 파일(`logs/backend_app_*.log`)로 자동 순환 기록되며 유지보수가 간편합니다.
*   **모바일 최적화 (PWA Ready)**
    *   데스크톱/모바일 UI 자동 반응형 대응, 다크/라이트 테마 지원, iOS/Android 기기에서 홈 화면 추가 시 네이티브 앱처럼 동작하는 쾌적한 UX를 제공합니다.

---

## 3. 필수 요구 사항 (Prerequisites)

*   Python 3.x 버전 이상
*   최신 웹 브라우저 (Chrome, Safari, Edge 등 권장)
*   (선택) 외부 접속을 위한 `ngrok`, `Cloudflare Tunnels` 또는 `tmux`

---

## 4. 설치 및 실행 가이드 (Installation & Execution)

### 서버 실행
프로젝트 폴더로 이동한 뒤, 아래 제공되는 스크립트를 통해 로컬 서버를 실행합니다.
스크립트 실행 시 필요한 필수 패키지(`Flask`, `Werkzeug`, `waitress`)가 없다면 자동으로 설치 여부를 묻고 진행합니다.

**Mac / Linux 환경**
최초 1회 실행 권한을 부여한 뒤, 쉘 스크립트 형태로 간편하게 실행할 수 있습니다.
```bash
chmod +x run.sh
./run.sh
```
*(기존 방식인 `python backend_app.py` 명령어로도 동일하게 실행 가능합니다.)*

**Windows 환경**
```bash
python backend_app.py
```

### 접속
웹 브라우저를 열고 다음 주소로 접속합니다.
```text
http://127.0.0.1:5000
```

---

## 5. 데이터 백업 및 보안 가이드 (Backup & Security)

### 데이터 백업 및 복구
*   **웹에서 간편하게 백업 (권장)**: 앱 화면 상단의 **[완벽 백업]** 버튼을 클릭하면 로그인한 계정의 전체 DB(`journal.db`)와 첨부 이미지가 포함된 무결성 검증 ZIP 파일이 다운로드됩니다. 새로운 환경에서 **[백업 원복]** 버튼을 통해 업로드하면 기존 상태 그대로 복원됩니다.
*   **자동 백업**: 매일 자정, 서버 내부(`backup/` 폴더)에 사용자별 백업 압축 파일이 자동 생성되며 100% 복원 가능한 상태인지 자체 무결성 검사(CRC, 레코드 수)를 수행합니다.
*   **수동 백업**: 직접 서버를 이전할 경우, 프로젝트 내의 `db/` 폴더와 `uploads/` 폴더만 복사하시면 됩니다.

### 보안 가이드 (Security Guide)
본 애플리케이션은 개인의 민감한 금융 및 투자 데이터를 다루므로 각별한 주의가 필요합니다.
*   **최고 관리자 계정**: 앱을 최초로 설치한 후 가장 처음으로 가입하는 계정이 자동으로 최고 관리자로 지정되며, 이후 가입하는 사용자는 이 관리자의 승인이 있어야 로그인이 가능합니다.
*   **세션 보안**: 내부적으로 세션 탈취(XSS) 및 크로스 사이트 요청 위조(CSRF) 방지 보안 쿠키 설정이 적용되어 있으며, 공용 기기에서의 데이터 유출을 막기 위해 1시간 동안 활동이 없으면 자동으로 로그아웃됩니다.
*   **외부 접속 주의**: 공유기 포트포워딩을 통한 직접적인 외부망(HTTP) 노출은 권장하지 않습니다. 외부(스마트폰 등)에서 안전하게 접속하려면 `ngrok`, `Cloudflare Tunnels`, `Tailscale`과 같은 암호화된 보안 터널링 서비스를 이용하시기 바랍니다.
*   **운영 환경(HTTPS)**: 로컬망에서는 `waitress`를 통해 안정적으로 구동되지만, 본격적으로 웹에 퍼블리싱 하시려면 Nginx 등의 리버스 프록시(Reverse Proxy)를 연동하여 HTTPS(SSL) 인증서를 적용하는 것을 적극 권장합니다.

---

## 6. 프로젝트 구조 (Project Structure)

```text
stock-memo/
├── backend_app.py      # 앱 구동·라우트·DB 헬퍼·백그라운드 스레드 (Flask)
├── prices.py           # 현재가(시세) 조회 서비스 (provider별 분해 + 폴백)
├── stats.py            # 매매 성과 분석(통계) 계산 로직 (순수 함수)
├── entry_logic.py      # 매매 기록 저장/무결성 검증 + INSERT 컬럼 단일 소스
├── trading_api.py      # 시스템 트레이딩 연동 REST API (/api/v1/*) — 인증·멱등·정규화
├── backups.py          # 백업 ZIP 무결성 검증 로직 (순수 함수)
├── templates/          # 로그인·회원가입 HTML 템플릿
├── stock-memo.html     # 프론트엔드 메인 화면 구조 (HTML)
├── style.css           # 화면 디자인 및 레이아웃 (CSS)
├── calc.js             # 매매 계산 단일 소스 (백엔드 stats.py 와 동일 알고리즘)
├── script.js           # 화면 동작, 데이터 통신, 차트 로직 (JavaScript)
├── run.sh              # 자동화 실행 쉘 스크립트 (Mac/Linux)
├── UniversalTradingHistoryAPI.json  # 트레이딩 봇 연동 API 계약 (OpenAPI 3.1)
├── backup/             # 매일 자동 생성되는 사용자별 백업 파일(ZIP) 저장 폴더
├── db/                 # 데이터베이스 폴더
│   └── journal.db      # 자동 생성되는 매매 기록 데이터베이스 파일 (SQLite)
├── logs/               # 시스템 및 에러 로그가 저장되는 폴더
│   └── backend_app.log # 디버깅/에러/경고 등 서버 실행 로그 파일
└── uploads/            # 첨부된 이미지 파일이 저장되는 폴더
```

> 백엔드는 도메인별 모듈(`prices`/`stats`/`entry_logic`/`backups`)로 분리되어 있으며,
> 손익 계산은 프론트엔드 `calc.js` 와 백엔드 `stats.py` 가 **동일한 이동평균단가
> 알고리즘**을 사용하도록 단일화되어 있습니다 (`tests/calc.test.js` 가 양쪽 일치를 검증).

---

## 7. 시스템 트레이딩 연동 API (Trading API)

외부 트레이딩 봇(HTS)이 체결 내역을 이 서버에 실시간으로 기록할 수 있는 REST API를 제공합니다.
전체 계약은 [`UniversalTradingHistoryAPI.json`](UniversalTradingHistoryAPI.json) (OpenAPI 3.1)에 정의되어 있습니다.

### 인증

1. 웹 대시보드 → 설정 → **HTS 연동 API 키**에서 키를 발급받습니다. (`skm_`로 시작)
2. 키를 `X-API-KEY` 헤더에 담아 `POST /api/v1/auth/token` 을 호출해 24시간 유효한 Access Token으로 교환합니다.
3. 이후 모든 요청에 `Authorization: Bearer <token>` 을 붙입니다.

> ⚠️ **키 원문은 발급 직후 한 번만 표시됩니다.** 서버에는 SHA-256 해시만 저장되어 다시 조회할 수 없으므로, 발급 시점에 복사해 두세요.
> 키를 폐기하면 그 키로 발급된 토큰도 **즉시** 무효화됩니다.

### 스코프

키에는 스코프가 부여되고 토큰이 이를 상속합니다.

| 스코프 | 허용 동작 |
|---|---|
| `trades:write` | 매매 기록 생성·수정·삭제, 기초잔고 등록 |
| `trades:read` | 매매 기록 조회, 동기화 지점 조회 |
| `bot:write` | 봇 상태(Ping) 전송 |

### 주요 엔드포인트

| 메서드 | 경로 | 설명 |
|---|---|---|
| `GET` | `/api/v1/health` | 서버 상태 확인 (무인증) |
| `POST` | `/api/v1/auth/token` | API 키 → Access Token 교환 |
| `POST` | `/api/v1/trades` | 매매 기록 단건 전송 |
| `POST` | `/api/v1/trades/batch` | 일괄 전송 (최대 500건, 항목별 결과 반환) |
| `GET` | `/api/v1/trades` | 기록 조회 (대사/Reconcile용, 커서 페이지네이션) |
| `GET` | `/api/v1/trades/last-sync` | 마지막 동기화 지점 (백필 구간 계산) |
| `GET` | `/api/v1/trades/by-exec-id/{id}` | 멱등키로 저장 여부 확인 |
| `PATCH` / `DELETE` | `/api/v1/trades/{id}` | 기록 정정 / 삭제 |
| `POST` | `/api/v1/positions/opening` | 연동 시작 시점의 기초 보유 잔고 등록 |
| `POST` | `/api/v1/bot/status` | 봇 가동 상태 Ping |

### 설계 원칙

*   **멱등성**: `brokerExecutionId`에 UNIQUE 제약이 걸려 있어 중복 재전송은 새 기록을 만들지 않고 기존 기록을 `200`으로 돌려줍니다. 따라서 봇은 응답을 못 받았을 때 **무조건 재전송해도 안전**합니다.
    권장 키 형식은 `{env}:{계좌}:{체결일}:{주문번호}` 입니다 — 증권사 주문번호는 영업일마다 재사용되므로 주문번호 단독 사용은 금지합니다.
*   **유실 금지**: 봇이 보낸 체결은 보유 수량 초과 매도 같은 무결성 위반이 있어도 **저장하고 `needsReview`로 표시**합니다. `400`으로 되돌리면 봇이 재시도해도 계속 실패해 그 체결이 영구히 사라지기 때문입니다. (웹 UI에서 사람이 직접 입력하는 경우는 즉시 고칠 수 있으므로 종전대로 차단합니다.)
*   **모의/실거래 분리**: `isSimulated` 플래그로 분리 저장하며 기본 조회·통계에서 제외됩니다.
*   **시스템/수동 분리**: `isSystem` 플래그로 자동매매가 낸 주문만 `시스템`으로 분류합니다. 봇은 자기 계좌의 체결을 **전부** 보고하므로(증권사 앱·HTS에서 사람이 낸 주문 포함), 이 구분이 없으면 자동매매 성과와 수동 매매가 한 덩어리가 됩니다. → [매매 분류](#매매-분류--무엇이-시스템인가)
*   **거래일 귀속**: `executedAt`은 오프셋 포함 RFC3339로 받고, `exchange`와 함께 **거래소 현지 거래일**(`tradeDate`)을 산출합니다. 미국 애프터마켓 체결이 한국 날짜로 밀리지 않습니다.
*   **레이트 리밋**: 토큰 발급은 IP당 5분 10회(무차별 대입 차단), 일반 API는 키당 1분 600회. 초과 시 `429` + `Retry-After`.

### 매매 분류 — 무엇이 '시스템'인가

봇은 자기 계좌에서 일어난 체결을 **전부** 보고합니다. 토스 앱이나 증권사 HTS에서 사람이 직접 낸 주문도 잔고 대조로 감지되어 함께 올라갑니다. 예전에는 `tradeClass`가 비면 서버가 `시스템`으로 채웠는데, 그 탓에 그 전부가 자동매매 성과로 뭉쳐졌습니다.

지금은 봇이 보내는 `isSystem`으로 가릅니다.

| `isSystem` | 서버가 매기는 분류 |
|---|---|
| `true` | `시스템`으로 확정 |
| `false` | 같은 종목의 직전 분류를 상속 → 없으면 빈 값 |
| *(필드 없음)* | 위와 동일 (봇이 출처를 모르는 경우) |

`tradeClass`를 봇이 직접 지정하면 그 값을 그대로 씁니다.

**상속 규칙**: 같은 사용자·종목의 가장 최근 기록에서 분류를 물려받되, **`시스템`은 물려받지 않습니다.** 예전 버전이 봇 기록을 전부 `시스템`으로 저장해 뒀기 때문에, 상속하면 고치려던 오염이 영구화됩니다. 상속은 사람이 실제로 뜻을 담아 고른 분류(`장기투자`·`배당투자` 등)에만 걸립니다.

기초잔고(`POST /api/v1/positions/opening`)도 `isSystem=false`입니다 — 연동 이전부터 들고 있던 것이지 자동매매가 낸 체결이 아닙니다.

> 이미 `시스템`으로 저장된 과거 기록은 그대로 남습니다. 멱등 처리 때문에 재동기화로도 덮이지 않으므로 웹에서 직접 고쳐야 합니다.

### 여러 대의 봇 (botId)

하트비트와 명령의 스코프는 **API 키가 아니라 사용자**입니다 — 키는 인증 직후 username으로 바뀌고 그 뒤로는 어느 키였는지 잊습니다. 그래서 HTS를 여러 대 돌릴 때 키를 따로 발급해도 인스턴스는 갈라지지 않습니다.

`botId`(Ping 본문)가 그 구분자입니다. HTS 쪽에서는 `JOURNAL_BOT_ID` 환경변수로 지정합니다.

*   **상태**: `bots` 테이블에 인스턴스별로 기록됩니다. 웹 표시등의 대표 상태는 **가장 나쁜 봇**을 따릅니다 — '하나라도 살아 있으면 초록'으로 하면 실전봇의 죽음이 모의봇 Ping에 가려지기 때문입니다. 어느 봇인지는 표시등 아래 목록에서 확인합니다.
*   **명령**: 재동기화는 대상 봇을 지정해 큐에 넣습니다. 봇이 둘 이상인데 대상을 비우면 서버가 `400`으로 거절합니다 — 명령은 한 번만 전달되므로(at-most-once) 먼저 Ping한 아무 봇이나 채가고, 그 봇이 ack까지 보내 화면에는 '완료'로 뜨는 **조용한 실패**가 되기 때문입니다.
*   **하위 호환**: `botId`를 보내지 않는 구버전 봇은 `default`로 묶입니다. 대상을 지정하지 않은 명령은 봇이 한 대뿐일 때만 전달됩니다.

### 재동기화 — 지운 기록 되살리기

웹에서 기록을 지우면 **봇이 자동으로 다시 보내지 않습니다.** 봇은 "내가 보냈다"는 사실만 기억할 뿐 서버에 지금도 있는지는 확인하지 않기 때문이고, 이게 맞는 동작입니다 — 의도적으로 지운 기록이 계속 되살아나면 지울 방법이 없어집니다.

되살리려면 **설정 → 계정 설정 → 재동기화** 버튼을 씁니다. 분기(90일)/반기(180일)/1년(365일) 중에 고르면 해당 기간의 체결을 봇이 다시 보냅니다. (모두 달력이 아닌 **롤링** 기준 — 분기 초에 누르면 달력 분기는 며칠뿐이라 정작 지워진 구간을 못 덮습니다.)

> API(`POST /api/me/bot/resync`)는 `from`/`to` 직접 지정도 받습니다. 화면에 입력칸을 두지 않은 것은 프리셋으로 충분하기 때문이지, 봇이 임의 구간을 못 다뤄서가 아닙니다.

**중복은 생기지 않습니다.** `brokerExecutionId` 멱등 처리로 이미 있는 기록은 `duplicate`로 건너뜁니다. 90일을 재동기화했는데 실제로는 10일치만 지워져 있었다면 `inserted=10, skipped=80`이 돌아옵니다. 이 두 숫자가 곧 **무엇이 얼마나 지워져 있었는지에 대한 답**이라 웹에도 나눠 표시합니다. 중복이 공짜이니 기간은 넉넉히 잡는 편이 낫습니다.

#### 명령 전달 방식

봇은 대개 가정용 네트워크 뒤에 있어 **서버가 먼저 접속할 수 없습니다.** 그래서 버튼을 누르면 `bot_commands` 테이블에 요청이 쌓이고, 봇의 다음 Ping(최대 10초) 응답에 실려 전달됩니다.

```
POST /api/v1/bot/status  →  { "command": "resync", "commandId": 17,
                              "commandParams": { "from": "2026-05-04", "to": null } }
다음 Ping 요청 본문       ←  { "status": "running",
                              "commandAck": { "id": 17, "result": "queued", "count": 42 } }
```

*   **명령은 한 번만 내려보냅니다(at-most-once).** ack를 못 받아도 다시 보내지 않습니다.
    ack를 받을 때까지 반복 전달하면 '명령이 반드시 실행된다'는 보장은 얻지만, 봇이 명령을 받고 ack를 보내기 전에 재시작하면 **같은 재동기화가 한 번 더 돕니다.** 서버 데이터로는 멱등해도 **운용자의 의도로는 멱등하지 않습니다** — 두 실행 사이에 일부러 지운 기록이 되살아납니다. 전달이 유실됐을 때 버튼을 한 번 더 누르는 편이 훨씬 낫습니다.
*   **화면에 '완료'로 표시된 명령은 어떤 경우에도 다시 나가지 않습니다.** 봇의 중복 실행 방지는 메모리에만 있어 재시작하면 잊으므로, 이 보장은 서버가 집니다.
*   봇이 끝내 가져가지 않으면 1시간 뒤 만료되어 웹에 '미처리'로 표시됩니다 (대개 봇이 꺼져 있던 경우). 받아갔는데 결과 보고가 없으면 그때까지 '처리 중'입니다.
*   버튼을 여러 번 눌러도 대기 중인 같은 명령이 있으면 새로 만들지 않습니다.
*   `status=stopped`인 봇에는 일감을 주지 않습니다 — 받아도 처리하지 못합니다.
*   ack 형식이 잘못돼도 Ping 자체는 `200`으로 처리합니다. 여기서 `400`을 돌려주면 하트비트가 끊겨 화면이 '통신단절'로 바뀝니다.

#### 백업에서 복원한 뒤에는 재동기화가 필요합니다

백업 ZIP은 `data.json`(매매 기록) + 첨부 이미지 + `account_info.json`(계좌 매핑)을 담습니다. **`api_keys`와 `users` 테이블은 담기지 않습니다** — 백업은 "기존 계정에 데이터를 되돌리는" 용도이지 "빈 서버를 통째로 세우는" 용도가 아닙니다. 새 서버에 복원했다면 웹에서 API 키를 새로 발급하고 HTS의 `JOURNAL_API_KEY`를 교체해야 합니다.

그리고 **백업 시점 이후 봇이 보낸 기록은 복원으로 되살아나지 않습니다.** 봇은 '이미 보냈다'는 사실만 기억하므로 자동으로 다시 보내지 않고, 백필도 이 구간을 잡지 못합니다(봇 쪽 큐에는 전송 완료로 남아 있기 때문). **복원 직후 재동기화를 눌러야 메워집니다.** 요청 창구는 **설정 → 계정 설정의 '재동기화' 버튼 한 곳**입니다 — 기간을 정하고 진행 상태를 확인하는 흐름이 갈라지지 않도록, 복원 화면에서는 요청하지 않습니다.

재동기화는 봇의 로컬 거래기록을 원본으로 삼으므로, 봇 쪽 큐에서 이미 정리된 오래된 기록까지 복구됩니다.

> ⚠️ **`pause`/`resume`은 의도적으로 구현하지 않았습니다.** API 스펙 enum에는 있지만 `SUPPORTED_BOT_COMMANDS`에서 제외되어 큐에 넣는 것부터 거부합니다. 재동기화는 "이미 봇의 것인 데이터를 다시 보내라"이지만, `pause`는 **웹서버가 매매봇을 정지시키는 권한**입니다. 웹이 침해되거나 버그를 내면 포지션을 든 채로 봇이 멈춥니다. 필요하다면 재확인·자동 만료 같은 안전장치를 갖춘 별도 설계가 필요하며, 재동기화와 같은 취급을 해서는 안 됩니다.

### 오류 응답

```json
{ "error": "사람이 읽는 메시지", "errorCode": "OVERSELL", "details": {} }
```

분기는 반드시 `errorCode`로 하세요. `error` 문구는 예고 없이 바뀔 수 있습니다.

### 운영 시 주의

*   API 키가 평문으로 전송되지 않도록 **반드시 HTTPS**로 서비스하세요 (Nginx 등 리버스 프록시 + SSL).
*   레이트 리밋은 프로세스 내 메모리 기준입니다. 다중 프로세스로 확장할 때는 Redis 등 공유 저장소로 교체해야 합니다.

---

## 8. 테스트 가이드 (Testing)

백엔드 API와 데이터 무결성 검증, 백업 복원(라운드트립), 성과 분석 로직은 `pytest` 기반 단위 테스트로 검증됩니다.
테스트 코드는 `tests/` 폴더에 위치하며, 임시 DB를 사용하므로 실제 구동 데이터(`db/journal.db`)에 영향을 주지 않습니다.

```bash
# 백엔드 전체 테스트 실행
pytest

# 간결한 출력 모드로 실행
pytest -q
```

프론트엔드 계산 엔진(`calc.js`)은 Node 내장 테스트 러너로 검증하며, 백엔드 통계
테스트와 동일한 픽스처를 사용해 프론트=백엔드 결과 일치를 보장합니다.

```bash
# 프론트엔드 계산 단위 테스트 (Node 18+)
node --test tests/calc.test.js
```
